# Claude Code 投标分析指南

> 专为 Claude Code 设计的 MCP 服务器使用指南  
> 基于真实投标分析场景的最佳实践

## 你是 Claude Code，这是你的投标分析工具箱

这个 MCP 服务器为你提供了 **12 个专业工具**，帮助你自动分析投标文档。按照以下模式工作，可以高效完成分析任务。

---

## 🎯 核心工作模式

### 模式 1: 单投标方深度分析

当你需要深入分析一个投标方的响应时：

```
1. list_available_versions → 确认有哪些文档
2. get_document_outline → 了解文档结构，定位章节
3. search_chunks (带 page_range) → 在相关章节检索
4. get_chunk_with_context → 获取完整上下文
5. extract_key_value → 提取结构化承诺数据
```

### 模式 2: 多投标方对比分析

当你需要对比多个投标方的响应时：

```
1. list_available_versions → 获取所有版本
2. compare_across_versions → 对同一问题对比所有投标方
3. extract_key_value (对每个版本) → 提取可比较的数据
4. batch_search (可选) → 批量获取补充信息
```

### 模式 3: 特定维度批量调研

当你需要调研多个相关维度时：

```
1. get_document_outline → 快速了解结构
2. batch_search → 一次查询多个相关主题
3. filter_and_sort_results → 筛选高分结果
4. get_chunk_with_context → 深入查看重要发现
```

---

## 📋 实战场景示例

### 场景 1: 分析"售后服务"评分项

**你的目标**: 全面了解某个投标方的售后服务承诺

**推荐步骤**:

```python
# 第 1 步: 获取文档大纲，找到售后服务章节
outline = get_document_outline(
    version_id="33333333-3333-3333-3333-333333333333",
    max_depth=2
)
# 你会看到类似:
# - 售后服务方案 (pages 16-19)
# - 培训方案 (pages 20-22)

# 第 2 步: 在售后服务章节精确检索
results = search_chunks(
    version_id="33333333-3333-3333-3333-333333333333",
    query="售后响应时间 SLA 质保期",
    page_range=[16, 19],  # 只搜售后服务相关页
    top_k=5
)

# 第 3 步: 对高分结果获取完整上下文
top_result = results["results"][0]  # 假设分数最高
detail = get_chunk_with_context(
    chunk_id=top_result["chunk_id"],
    context_depth="section",  # 获取整个章节
    include_adjacent_pages=True
)
# 现在你有完整的上下文，不会断章取义

# 第 4 步: 提取结构化承诺数据
commitments = extract_key_value(
    version_id="33333333-3333-3333-3333-333333333333",
    key_patterns=[
        "响应时间", "到场时间", "质保期", "保修期",
        "工程师数量", "备件库存", "开机率"
    ],
    value_patterns=["小时", "天", "年", "月", "%", "人"],
    context_window=50
)
# 你会得到类似:
# [{"key": "响应时间", "value": "2小时", "page_idx": 17}, ...]
```

**你应该关注的关键点**:
- 响应时间承诺（2小时 vs 4小时 vs 8小时）
- 到场时间承诺（24小时 vs 48小时）
- 质保期限（3年 vs 5年 vs 终身）
- 开机率保证（95% vs 98%）
- 工程师团队规模
- 备件库存策略

---

### 场景 2: 对比三个投标方的售后服务

**你的目标**: 找出三个投标方在售后服务方面的差异

**推荐步骤**:

```python
# 第 1 步: 对比同一查询在所有版本的结果
comparison = compare_across_versions(
    version_ids=[
        "33333333-3333-3333-3333-333333333333",  # A
        "44444444-4444-4444-4444-444444444444",  # B
        "55555555-5555-5555-5555-555555555555"   # C
    ],
    query="售后响应时间 SLA 到场时效",
    top_k_per_version=3,
    normalize_scores=True  # 分数归一化，便于比较
)

# 返回结构:
# {
#   "query": "售后响应时间 SLA 到场时效",
#   "versions_compared": ["uuid-a", "uuid-b", "uuid-c"],
#   "results_by_version": {
#     "uuid-a": [{"text": "2小时响应...", "score": 0.95, "normalized_score": 1.0}],
#     "uuid-b": [{"text": "4小时响应...", "score": 0.88, "normalized_score": 0.85}],
#     "uuid-c": [{"text": "8小时响应...", "score": 0.72, "normalized_score": 0.65}]
#   }
# }

# 第 2 步: 对每个版本提取结构化数据
all_commitments = {}
for version_id in ["uuid-a", "uuid-b", "uuid-c"]:
    commitments = extract_key_value(
        version_id=version_id,
        key_patterns=["响应时间", "到场时间", "质保期"],
        value_patterns=["小时", "天", "年"]
    )
    all_commitments[version_id] = commitments

# 第 3 步: 整理对比表格
# 你应该输出类似这样的对比:
# | 指标 | 投标方A | 投标方B | 投标方C |
# |------|---------|---------|---------|
# | 响应时间 | 2小时 | 4小时 | 8小时 |
# | 到场时间 | 24小时 | 48小时 | 72小时 |
# | 质保期 | 5年 | 3年 | 3年 |
```

**你应该输出的分析格式**:

```markdown
## 售后服务对比分析

### 响应时效对比
- **投标方A** (上海澄研): 2小时响应，24小时到场 ⭐最佳
- **投标方B** (苏州启衡): 4小时响应，48小时到场
- **投标方C** (杭州赛泓): 8小时响应，72小时到场

### 质保期对比
- **投标方A**: 5年 (60个月) ⭐最佳
- **投标方B**: 3年 (36个月)
- **投标方C**: 3年 (36个月)

### 关键差异点
1. 响应时效方面，A方明显优于B、C
2. 质保期方面，A方提供最长保障
3. 建议优先考虑A方的售后服务方案
```

---

### 场景 3: 批量提取技术参数

**你的目标**: 快速提取设备的技术参数用于对比

**推荐步骤**:

```python
# 第 1 步: 批量搜索多个技术参数
batch_results = batch_search(
    version_id="33333333-3333-3333-3333-333333333333",
    queries=[
        "分辨率 清晰度",
        "扫描速度",
        "检测精度",
        "通道数 探头数",
        "辐射剂量"
    ],
    top_k_per_query=2,
    aggregate_by="query"  # 按查询分组，便于查看
)

# 第 2 步: 对每个参数提取数值
for query in batch_results["results"]:
    print(f"\n【{query}】")
    for result in batch_results["results"][query]:
        print(f"  Page {result['page_idx']}: {result['text'][:100]}...")
        
# 第 3 步: 提取结构化数值
tech_specs = extract_key_value(
    version_id="33333333-3333-3333-3333-333333333333",
    key_patterns=[
        "分辨率", "扫描速度", "精度", "通道数",
        "剂量", "灵敏度", "特异性"
    ],
    value_patterns=[
        "μm", "nm", "mm", "ms", "s",
        "dpi", "lpi", "%", "MP", "GB"
    ],
    context_window=30
)
```

---

## 🛠️ 工具使用技巧

### 技巧 1: 组合过滤获取精准结果

```python
# 先搜索，然后过滤
results = search_chunks(
    version_id="uuid",
    query="培训计划",
    top_k=20  # 多获取一些用于过滤
)

# 过滤：只要表格，且分数>0.7
filtered = filter_and_sort_results(
    results=results["results"],
    filters={
        "min_score": 0.7,
        # "element_types": ["table"]  # 如果支持的话
    },
    sort_by="page_idx",  # 按页码排序便于阅读
    sort_order="asc"
)
```

### 技巧 2: 利用上下文避免误解

```python
# ❌ 不要只引用片段
result = search_chunks(...)
quote = result["results"][0]["text"]  # 可能断章取义

# ✅ 应该获取完整上下文
result = search_chunks(...)
detail = get_chunk_with_context(
    chunk_id=result["results"][0]["chunk_id"],
    context_depth="section"  # 或 "paragraph"
)
# 现在 detail["text"] 是完整文本
# detail["hierarchy"] 显示章节层级
# detail["same_page_chunks"] 显示同页其他内容
```

### 技巧 3: 批量查询提高效率

```python
# ❌ 不要逐个查询（慢）
for query in ["质保期", "响应时间", "培训天数"]:
    result = search_chunks(version_id, query)  # 3次调用

# ✅ 使用 batch_search（快）
batch_results = batch_search(
    version_id=version_id,
    queries=["质保期", "响应时间", "培训天数"],
    aggregate_by="query"  # 一次调用，结果分组
)
```

### 技巧 4: 使用文档大纲快速定位

```python
# 获取大纲
outline = get_document_outline(version_id="uuid", max_depth=2)

# 快速找到你关心的章节
for item in outline["outline"]:
    if "售后" in item.get("heading", ""):
        print(f"售后服务在 pages {item['page_range']}")
        # 然后在这个范围内搜索
        results = search_chunks(
            version_id="uuid",
            query="响应时间",
            page_range=item["page_range"]  # 限定范围
        )
```

---

## ⚠️ 常见陷阱与避免方法

### 陷阱 1: 只看片段，误解原意

**问题**: 只使用 `search_chunks` 返回的 text 字段，可能断章取义。

**解决**: 总是使用 `get_chunk_with_context` 获取完整上下文。

```python
# ❌ 错误
result = search_chunks(...)
text = result["results"][0]["text"]
# 可能只抓到"不提供"而没看到前面的"第三方软件"

# ✅ 正确
result = search_chunks(...)
detail = get_chunk_with_context(
    chunk_id=result["results"][0]["chunk_id"],
    context_depth="section"
)
full_text = detail["text"]  # 完整段落
```

### 陷阱 2: 跨版本分数直接比较

**问题**: 不同版本的分数基于不同文档内容，不能直接比较。

**解决**: 使用 `compare_across_versions` 的 `normalize_scores=True`。

```python
# ✅ 正确：使用归一化分数
comparison = compare_across_versions(
    version_ids=["a", "b", "c"],
    query="售后服务",
    normalize_scores=True  # 分数归一化到 0-1
)
```

### 陷阱 3: 忽略版本ID的固定值

**问题**: 每次都要查询版本 ID。

**解决**: 使用固定的测试版本 ID。

```python
# 这些是固定的测试版本，直接使用：
VERSION_A = "33333333-3333-3333-3333-333333333333"
VERSION_B = "44444444-4444-4444-4444-444444444444"
VERSION_C = "55555555-5555-5555-5555-555555555555"
```

---

## 📊 你应该输出的分析格式

### 评分项分析模板

```markdown
## 【评分项名称】分析

### 招标要求
- 要求1: ...
- 要求2: ...

### 投标方响应对比

| 投标方 | 响应内容 | 符合度 | 页码 |
|--------|----------|--------|------|
| A (上海澄研) | ... | ⭐⭐⭐⭐⭐ | P17 |
| B (苏州启衡) | ... | ⭐⭐⭐⭐ | P18 |
| C (杭州赛泓) | ... | ⭐⭐⭐ | P16 |

### 关键差异点
1. ...
2. ...

### 建议评分
- A方: X分 (理由...)
- B方: Y分 (理由...)
- C方: Z分 (理由...)
```

---

## 🔧 调试技巧

当你遇到问题：

1. **检查版本ID是否正确**
   ```python
   versions = list_available_versions()
   print(versions)  # 确认UUID存在
   ```

2. **检查是否有数据**
   ```python
   outline = get_document_outline(version_id="uuid")
   print(len(outline["outline"]))  # 应该>0
   ```

3. **扩大搜索范围**
   ```python
   # 如果没结果，试试不限制页码
   results = search_chunks(version_id, query, page_range=None)
   ```

4. **检查查询词**
   ```python
   # 试试不同的查询词
   queries = ["响应时间", "响应时效", "SLA", "到场时间"]
   batch_results = batch_search(version_id, queries)
   ```

---

## 📝 记住这些要点

1. **总是先 Discovery** → 了解有什么文档
2. **使用 page_range** → 提高搜索精度
3. **获取上下文** → 避免断章取义
4. **批量查询** → 提高效率
5. **归一化对比** → 公平比较
6. **提取结构化数据** → 便于制作对比表
7. **引用页码** → 方便验证

---

## 🚀 开始你的第一次分析

```python
# 1. 列出可用文档
versions = list_available_versions(include_stats=True)

# 2. 获取第一个版本的文档大纲
outline = get_document_outline(
    version_id=versions["versions"][0]["version_id"],
    max_depth=2
)

# 3. 搜索你关心的内容
results = search_chunks(
    version_id=versions["versions"][0]["version_id"],
    query="售后服务 响应时间",
    top_k=5
)

# 4. 获取详细上下文
detail = get_chunk_with_context(
    chunk_id=results["results"][0]["chunk_id"],
    context_depth="section"
)

# 5. 开始你的分析！
print(detail["text"])
```

---

**祝你分析顺利！** 🎯
